# Required: Encryption key for router passwords (min 32 chars)
SECRET_KEY=change-this-to-a-random-32-byte-key

# Optional: Database URL (default: sqlite in current directory)
# DATABASE_URL=sqlite:///./mikrosms.db

# Optional: Debug mode
# DEBUG=false
